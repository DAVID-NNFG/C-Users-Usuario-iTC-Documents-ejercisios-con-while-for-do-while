#include <studio.h>

int main ()
    int a,b,c=0;
    do{
        cout<<"ingrese un numero"; cin>>a;
    }while (a < 0);
    do{
        cout<<"ingrese un nuemro" cin>>b;
        while( be < 0);

        for(int i=1; i<=b; i++){
            c = c + a;
        }
        cout<<"el resultado es";
        return 0
    }


int main ()
    int a,b,c=0;
    do{
        cout<<"ingrese un numero"; cin>>a;
    }while (a < 0);
    do{
        cout<<"ingrese un nuemro" cin>>b;
        while( be < 0);

        for(int i=1; i<=b; i++){
            c = c + a;
        }
        cout<<"el resultado es";
        return 0
    }


int main ()
    int a,b,c=0;
    do{
        cout<<"ingrese un numero"; cin>>a;
    }while (a < 0);
    do{
        cout<<"ingrese un nuemro" cin>>b;
        while( be < 0);

        for(int i=1; i<=b; i++){
            c = c + a;
        }
        cout<<"el resultado es";
        return 0
    }