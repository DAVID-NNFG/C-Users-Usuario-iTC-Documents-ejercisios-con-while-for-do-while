#include <stdio.h>

int main (){
    // se asigna la variable 
    int i,suma = 0; 

    for ( i=0;1<=10;i+=2){
        suma += i,
    }
    // se realisar el calcul0 

    prinf("\n la suma de los dos numeros pares es: %1" , suma);

    return 0 
}


int main (){
    // se asigna la variable 
    int i,suma = 0; 

    while ( i=0;1<=10;i+=2){
        suma += i,
    }
    // se realisar el calcul0 

    prinf("\n la suma de los dos numeros pares es: %1" , suma);

    return 0 
}



int main (){
    // se asigna la variable 
    int i,suma = 0; 

    do while ( i=0;1<=10;i+=2){
        suma += i,
    }
    // se realisar el calcul0 

    prinf("\n la suma de los dos numeros pares es: %1" , suma);

    return 0 
}